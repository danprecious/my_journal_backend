import { Request, Response } from "express";
import { CreateUser } from "types/globalTypes.js";

export const createUserController = async (req: Request<CreateUser>, res: Response) => {
    try {
        
    } catch (error) {
        
    }
}